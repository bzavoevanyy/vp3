$(document).ready(function () {
    // jQuery test
    console.log('[ jQuery works ... ]');

    // Uploud images
    uploadImages.init();

    // Watermark Position Module init
    wPosition.init();
});